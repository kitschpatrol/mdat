{
	"name": "magicmark-monorepo",
	"version": "0.0.0",
	"type": "module",
	"description": "Use comments as templates in markdown files.",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/kitschpatrol/magicmark.git"
	},
	"homepage": "https://github.com/kitschpatrol/magicmark",
	"bugs": {
		"url": "https://github.com/kitschpatrol/magicmark/issues",
		"email": "eric@ericmika.com"
	},
	"author": {
		"name": "Eric Mika",
		"email": "eric@ericmika.com",
		"url": "https://ericmika.com"
	},
	"license": "MIT",
	"private": true,
	"engines": {
		"node": ">=18.0.0",
		"pnpm": ">=8.0.0"
	},
	"scripts": {
		"build": "pnpm -r run build",
		"clean": "git clean -fdX",
		"prepublishOnly": "pnpm run build",
		"release": "pnpm bumpp -r --commit 'Release: %s' --tag 'v%s' && pnpm -r publish --otp $(op read 'op://Personal/Npmjs/one-time password?attribute=otp')"
	},
	"devDependencies": {
		"@kitschpatrol/shared-config": "^4.3.3",
		"bumpp": "^9.3.0"
	}
}
